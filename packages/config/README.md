# packages/config

Schemas de validação de variáveis de ambiente (ex. `zod`), compartilhados entre os apps para
garantir que nenhum serviço suba com configuração inválida/ausente.

**Status:** placeholder — criado na **FASE 3**, junto com o bootstrap da API (primeiro consumidor
real da validação de env).
