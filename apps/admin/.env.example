# Copie para .env.local nesta pasta (apps/admin/.env.local) antes de rodar o app.
# Variaveis NEXT_PUBLIC_* ficam embutidas no bundle JS - nunca coloque segredos aqui.

# URL base da API (apps/api), incluindo o prefixo /api/v1.
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1
