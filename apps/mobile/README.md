# apps/mobile

App do aluno em React Native + Expo + TypeScript (Android/iOS).

**Status:** placeholder — o projeto Expo real é criado na **FASE 10 (Aplicativo mobile)**, depois
que a API (auth, catálogo, progresso, assinaturas, player) já existir para ser consumida.

Estrutura planejada:

```
src/
  screens/
  components/
  features/   player, metronome, tuner
  services/   api client, storage local
```
