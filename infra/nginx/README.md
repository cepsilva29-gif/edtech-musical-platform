# infra/nginx

Configuração de reverse proxy para ambiente de produção (roteamento entre API e admin,
TLS termination, headers de segurança).

**Status:** placeholder — criado na **FASE 13 (Docker e produção)**.
