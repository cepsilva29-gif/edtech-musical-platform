# infra/docker

Dockerfiles por serviço (`api`, `admin`) e `docker-compose` de desenvolvimento
(api + PostgreSQL + Redis, com volumes persistentes).

**Status:** placeholder — criado na **FASE 13 (Docker e produção)**, quando houver aplicações
reais para containerizar.
